Now a filename correction across the emitted vault. Generated node
filenames currently carry the typed ID prefix (repo:/table:). Change them
to clean, bare titles — the folder already encodes the type:
- nodes/repos/<title>.md          (fraud-scorer.md, not repo:fraud-scorer.md)
- nodes/packages/<title>.md       (bare title)
- nodes/tables/<db>.<table>.md    (keep db qualifier — tables can collide across dbs)
Keep the typed id (repo:foo) in frontmatter ONLY. Update: the emit
templates, the golden reference in docs/fleet-reference/, all generated
relative links (nodes, hubs, dashboards, bases, views) to point at the new
filenames, and any seeded bookmarks/graph filters that referenced old
paths. Then re-verify determinism (emit twice = empty diff) and
link-integrity (zero dangling references). Plan mode first — show me the
blast radius (how many link sites change) before editing. Commit
separately when done.