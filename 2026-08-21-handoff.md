Read SPEC-ADDENDUM-A.md V1.1(d) including the reference additions, and
browse docs/fleet-reference/ — golden target, match its layout, frontmatter,
body order, callout patterns, CSS, and assets exactly unless the addendum
contradicts it (addendum wins). Sandbox denies any path segment starting
with "vault"; emit target is ../fleet-map, config key vault_path may keep
its name.

Implement cluster (d): --init scaffolding (idempotent, skip-if-exists:
.obsidian seeds incl. both CSS snippets + graph color groups, templates/,
CLAUDE.md, USING.md, control-note skeletons, .command action scripts,
Demo.md, workspace note); CONTRACT.md already exists from (c) — extend if
needed per reference (schema + ownership map + glossary); dashboards (Home
operator + README public from one template, needs-curation section); emit
fleet.base + curation.base; emit assets/*.svg charts (health donut, crit
trend from signals-history, campaign burndown); --palette ops|soft param
(default: ops); dashboard desk param shipping home + security desk.
cosmo doctor. Plan mode first if anything touches the F3 ownership guard's
allowlist (new writable paths). Commit per checkbox, run fixtures per
checkbox, stop at cluster end.