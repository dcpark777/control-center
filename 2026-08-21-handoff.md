First run: ls docs/fleet-reference/nodes/repos — confirm you can read the
reference; stop and tell me if not. Then read SPEC-ADDENDUM-A.md §0.5 and
V1.1(c), the gate decisions in V1.0-REPORT.md, and browse
docs/fleet-reference/ — it is the golden rendering target: match its file
layout, frontmatter, body section order, callout patterns, and link style
exactly unless the addendum contradicts it (addendum wins).

IMPORTANT sandbox constraint: this machine's filesystem denies any path
segment starting with "vault". The emit target directory is ../fleet-map
(set vault_path in engine config to ../fleet-map). Never create or read a
path containing "vault" as a directory name.

Implement ONLY cluster (c): emit core — jinja2 templates per node type,
determinism (emit twice = empty diff), ownership guard via path allowlist,
render-then-write atomicity, orphan deletion, filename sanitization,
colon→dot ID mapping, emit-side hub synthesis, link-integrity report,
local git init of ../fleet-map + commit-on-refresh. Plan mode first for
the ownership guard (F3). Commit per checkbox, run relevant Gate-A
fixtures per checkbox. Stop at cluster end.