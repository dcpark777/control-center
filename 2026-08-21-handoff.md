Opening the real ../fleet-map vault in Obsidian surfaced three issues.
Fix all three, then re-emit under the new live-vault safety gate.

1. --init scaffolding never landed in ../fleet-map: missing
   .obsidian/snippets/cosmo.css + cosmo-soft.css, fleet.base +
   curation.base, templates/, Demo.md. The content emit (nodes, Home,
   charts) ran but the authored-seed layer didn't. Diagnose why: did --init
   run at all, or did the fleet/<type> restructure / legacy sweep remove or
   relocate the seeds? Then make --init seed them correctly. It MUST be
   idempotent skip-if-exists so it never clobbers files I've edited. Match
   docs/fleet-reference/ for the seed contents.

2. Node frontmatter shows "Invalid properties" in Obsidian. The deps map
   emits Python-style values — bare `None` and unquoted version specifiers
   like ==1.3.*, >=2022.12.7 — which Obsidian's YAML parser rejects. Emit
   valid YAML: quote version strings, use null or omit instead of None,
   and confirm the deps structure parses as real Obsidian properties.
   Check ALL node types, not just repos. Add a test asserting emitted
   frontmatter is valid YAML.

3. Home's board embed shows "'fleet.base' is not created yet" — once #1
   seeds fleet.base, confirm Home's embed path matches where it actually
   lands so the board renders.

LIVE-VAULT SAFETY GATE (standing policy now): do NOT run emit/init directly
against ../fleet-map. Dry-run to a scratch dir, show me the diff and a
summary of what would change in the real vault, and WAIT for my explicit
"yes, run it on ../fleet-map" before touching it. After I approve and you
run it, re-verify determinism (emit twice = empty diff) and valid YAML.
Plan mode first for the --init diagnosis.