Don't omit — that hides the repo from "which repos use <pkg>" queries.
Keep version-less deps but mark them unresolved: emit deps entries so
resolved-version and version-unknown are distinguishable (e.g. version:
null with resolved: false, or a separate deps_declared list). "Which repos
declare <pkg>" must find them; "which repos on <pkg> <range>" must exclude
them. If that's more than a small change, fall back to pkg: null rather
than omitting. Never drop the entry.