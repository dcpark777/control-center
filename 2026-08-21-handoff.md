Context efficiency task — no code, just docs. Every session start reads
PROGRESS.md, SPEC.md, SPEC-ADDENDUM-A.md and it's eating ~10% of context.
Most of that is history a fresh session doesn't need. Restructure for
"small always-read core, detailed history on demand":

1. PROGRESS.md is the main offender if it's an append-only session log.
   Split it:
   - Keep a SHORT "Current State" section at the TOP (≤40 lines): what's
     built, what phase we're in, what's next, known issues, the standing
     policies (live-vault gate, etc). This is the only part meant to be
     read every session.
   - Move the per-session historical log into PROGRESS-ARCHIVE.md (or
     docs/history/). Leave a one-line pointer in PROGRESS.md: "full session
     history in PROGRESS-ARCHIVE.md — read only if you need backstory."
   - Going forward, session entries append to the ARCHIVE, and only the
     Current State header in PROGRESS.md gets updated.

2. Check whether CLAUDE.md instructs reading all these files in full on
   every session. If so, change the instruction to: read PROGRESS.md
   (Current State only) always; read SPEC.md / SPEC-ADDENDUM-A.md
   /ARCHIVE only when the task needs them. A quick task shouldn't require
   loading the full spec.

3. SPEC.md and ADDENDUM: don't rewrite them, but add a short table-of-
   contents / section-index at the top of each so a session can jump to
   the relevant section instead of reading the whole thing. If either has
   large fully-completed phases, those can move to an appendix the TOC
   points at.

Show me the new PROGRESS.md Current State section before finalizing — I
want to confirm it captures the true current state concisely. Don't lose
any information; archive, don't delete. Commit when I approve.