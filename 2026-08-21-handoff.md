Change of plan before you finalize: use a fully uniform, generalizable
structure instead of the node-only rename. Emit all node types under a
single parent as fleet/<type>/<name>.md — fleet/repos/, fleet/tables/,
fleet/packages/, fleet/categories/, fleet/teams/, fleet/databases/. Bare
filenames everywhere (folder encodes type, so no repo:/cat: prefixes on
any of them, hubs included). Keep the db qualifier for tables
(fleet/tables/<db>.<table>.md) since tables can collide across databases.

Crucially: the set of node types must come from ONE place (a type registry
constant or config section), not be hardcoded per-template, so adding a new
node type later is one config entry that produces one folder — this is for
future multi-team/multi-domain use where types differ. Types map to folders
uniformly with no special cases.

Update SPEC-ADDENDUM-A §1 rule 1 and §2.9 to describe fleet/<type>/<name>.md
and the type-registry approach; note in PROGRESS. Re-plan the blast radius
against this final structure (all link sites, hub synthesis output paths,
seeded bookmarks/graph filters, the golden reference), then re-verify
determinism + link-integrity. Show me the updated blast radius before editing.