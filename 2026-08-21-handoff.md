Restructure SPEC-ADDENDUM-A.md's V1.1 checklist into headed subsections
matching how we've been executing: "V1.1(a) — predicate/classifier/config"
(done), "V1.1(b) — meta ingestion + F5 precedence" (done, includes the D1
mirror-backed carry-over), "V1.1(c) — emit core", "V1.1(d) — init,
dashboards, docs", "V1.1(e) — --fast + CLI agent contract". Move each
existing checklist line under its cluster, checked where complete per
PROGRESS.md; keep wording intact. Move the deferred --init-seeding note
under (d). Then ADD these lines under (d):

- [ ] Emit assets/*.svg (health donut, crit trend from signals-history, campaign burndown) per docs/vault-reference/assets/
- [ ] Emit fleet.base + curation.base per reference; Home embeds fleet.base
- [ ] --palette ops|soft emit param selects chart set + enabled CSS snippet (both seeds committed)
- [ ] Seed .command action scripts, Demo.md, workspace note per reference
- [ ] Dashboard template takes a desk param; ship home + security desk; deps/flow/jira pull-driven

And append to the V2 list:

- [ ] Run console: cosmo listen (whitelist: refresh, refresh --fast, emit) + live run-status note (append-exempt from atomicity, vault-gitignored), Home embed
- [ ] Mend-issues collector: GitHub issues per repo (findings w/ CVE, age, state, URL); check current criticals source before replacing
- [ ] Jira collector: API confirmed reachable; JQL per curated jira_component (meta/ field); interim: emitted JQL link-out buttons

Also add to SPEC.md's decision record: "Obsidian is the demo surface
(dashboard + graph + Demo.md); SPA reanimation trigger narrowed to
interactive-instrument need only." Commit both files:
"spec: cluster-structured V1.1, reference additions for (d), V2 collectors,
demo-surface decision". Don't push; I will.
